# Bubbit Hub Modern Kitsch UI Skin

- 기능 JS/Firebase 로직은 유지하고 `style.css` 중심으로 모던 키치 스타일을 적용했습니다.
- 업로드된 HTML 파일명은 실제 배포/링크에 맞게 정리했습니다.
- `content.html`, `login.html`, `firebase.js`는 이번 업로드 묶음에 없어 포함하지 않았습니다.
